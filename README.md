## Getting your Plex Token
1. Open [Plex Web](https://app.plex.tv/).
2. Click on the three dots on any item (movie, show, song, etc.) → "Get Info" → "View XML".
3. In the URL, find the `X-Plex-Token` parameter. This is your Plex Token.